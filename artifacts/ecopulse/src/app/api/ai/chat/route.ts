/**
 * Next.js API route stub — not executed in the Vite SPA.
 * AI chat is called from the client via the Genkit SDK directly
 * or a separate Express API server in artifacts/api-server.
 */
export {};
